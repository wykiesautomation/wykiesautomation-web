async function loadProducts(){const cfg=await (await fetch('config.json')).json();const res=await fetch(cfg.appsScriptUrl,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:'get-products','X-Auth':cfg.xAuthToken})});const payload=await res.json();const items=payload.data||[];const cards=document.getElementById('cards');cards.innerHTML=items.map(p=>`<article class="card"><img src="${p.image}" alt="${p.name}"><div class="content"><div class="price">${p.price}</div><h3>${p.name}</h3><p>${p.summary}</p><div style="display:flex;gap:8px;margin-top:8px"><a class="btn" href="product.html?id=${p.id}">View</a><a class="btn secondary" href="#">Free Trial</a></div></div></article>`).join('')}function filterCards(){const q=document.getElementById('q').value.toLowerCase();document.querySelectorAll('#cards .card').forEach(c=>{const text=c.innerText.toLowerCase();c.style.display=text.includes(q)?'block':'none'})}loadProducts();