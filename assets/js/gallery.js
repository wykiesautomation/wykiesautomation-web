async function loadGallery(){const cfg=await (await fetch('config.json')).json();const res=await fetch(cfg.appsScriptUrl,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:'get-gallery','X-Auth':cfg.xAuthToken})});const items=(await res.json()).data||[];const el=document.getElementById('gallery');el.innerHTML=items.map(d=>`<article class="card"><img src="${d.url}" alt="${d.title||''}"><div class="content"><h3>${d.title||''}</h3></div></article>`).join('')}loadGallery();